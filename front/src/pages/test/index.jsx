// Import Sections
import { Email } from "@/components/UI/Email";

const Test = () => {
    return (
        <div id="main_container" className="relative pt-[84px]">
            <Email />
        </div>
    );
};

export default Test;
